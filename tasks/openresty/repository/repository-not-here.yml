---
# FILE:            {{ role_path }}/tasks/openresty/compile/compile-not-here.yml
# LICENSE:         Public Domain

# Message for who does not read documentation, but is reading the sources
- name: "haproxy | repository | repository-not-here: this feature is not here (see bootstrap/repositories/*)"
  fail:
  msg: >
    Trying to import wrong file? This message is a reminder that ALB/HAproxy
    does not manange the repositories, but at bootstrap/repositories-*
