galaxy_info:
  author: Emerson Rocha <rocha(at)ieee.org>
  description: Águia Pescadora Application Load Balancer (AP-ALB)
  company: Etica.AI
  license: Unlicense
  # Ansible 2.8 because we use Extended Loop control https://docs.ansible.com/ansible/latest/user_guide/playbooks_loops.html#extended-loop-variables
  min_ansible_version: 2.8
  platforms:
    - name: Ubuntu
      versions:
      - bionic
  # - name: Fedora
  #   versions:
  #   - all
  #   - 25
  # - name: SomePlatform
  #   versions:
  #   - all
  #   - 1.0
  #   - 7
  #   - 99.99

  galaxy_tags:
    - applicationloadbalancer
    - loadbalancer
    - reverseproxy
    # List tags for your role here, one per line. A tag is a keyword that describes
    # and categorizes the role. Users find roles by searching for tags. Be sure to
    # remove the '[]' above, if you add tags to this list.
    #
    # NOTE: A tag is limited to a single word comprised of alphanumeric characters.
    #       Maximum 20 tags per role.

dependencies: []
  # List your role dependencies here, one per line. Be sure to remove the '[]' above,
  # if you add dependencies to this list.
